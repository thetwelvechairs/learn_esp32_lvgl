#include "Flipper.h"

Flipper::Flipper() {}

void Flipper::Init() {
//    for (int n = 0; n < 4; n++) {
//        sprites.at(n).setSwapBytes(true);
//        sprites.at(n).createSprite(this->width, this->height);
//        sprites.at(n).pushImage(0, 0, this->width, this->height, flipperLD);
//    }
}

void Flipper::Move() {}

Flipper::~Flipper() = default;
